# AFC03 AIC8800 第三方许可状态

状态：**发布者已确认拥有公开转载/再分发许可，可以制作公开包**。

这份记录只覆盖本地随板资料中用于 AFC03 的 AIC8800 文件，不对其他来源或其他芯片变体作结论。

## 已观察到的证据

- 驱动树顶层 `Makefile` 标记 `SPDX-License-Identifier: GPL-2.0`；
- 驱动树顶层 `Kconfig` 标记 `SPDX-License-Identifier: GPL-2.0-only`；
- `aic_load_fw` 和 `aic8800_fdrv` 均声明 `MODULE_LICENSE("GPL")`；
- 源文件包含 AICSemi、RivieraWaves、Realtek 等多方版权声明；
- 随板驱动目录原始副本没有独立的 `LICENSE`、`COPYING` 或完整第三方清单，因此发布包额外附带 GPL-2.0 许可证和本状态说明；
- 发布者确认 11 个固件/配置文件及两个预编译模块允许公开转载；
- 两个预编译模块只与实测 `6.1.99 SMP mod_unload aarch64` 内核匹配。

## 当前发布依据

项目自有的安装器、命令清单和教程按项目许可证发布。发布者已在 2026-09-08 明确确认拥有以下随板资料的公开转载/再分发许可：

- `aic_load_fw.ko`；
- `aic8800_fdrv.ko`；
- `wifi/firmware/*`；
- 随板资料中的驱动源文件副本。

这些第三方文件仍保留各自原始版权和许可，不能被解释为由本项目的 AGPL 许可证重新授权。驱动源码随包提供，并附 GPL-2.0 许可证；固件以未经修改的形式分发。

## 公开发布必须保持的条件

1. 不修改或删除源文件中的版权、SPDX 和作者声明；
2. 同时提供驱动源码副本、GPL-2.0 许可证、精确内核版本和文件哈希；
3. 明确固件和预编译模块来自 AFC03 随板资料，项目仅做安装封装；
4. 不宣称适配其他内核、板型或 AIC8800 变体；
5. 确认包内不含账号、密码、SSID、MAC、IP、SSH key、个人路径或设备日志；
6. 后续若权利方撤回或修订许可，发布者应及时更新或下架对应文件。

网上存在多个第三方 AIC8800 镜像，但本包不以“他人已经上传”作为授权依据，而以发布者确认的转载许可为依据。收到许可争议时，应先暂停二进制下载并保留安装器源码与排障教程。
